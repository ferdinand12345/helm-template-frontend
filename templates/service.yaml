apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-template-frontend.fullname" . }}
  labels:
    {{- include "helm-template-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-template-frontend.selectorLabels" . | nindent 4 }}
